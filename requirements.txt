# Minimal by design: no agent frameworks (no LangChain/LangGraph/AutoGen/CrewAI).

# HTTP client for the llama.cpp servers.
requests>=2.32,<3

# The API the React front end talks to (api/). The CLI (main.py) needs
# neither, and runs with requests alone.
fastapi>=0.121
uvicorn[standard]>=0.34

# Multipart form parsing, for POST /api/uploads. Not optional and not a test
# dependency: FastAPI inspects the route signatures at import time, so without
# this the application object cannot even be built - a fresh clone fails at
# startup rather than when someone first attaches an image.
python-multipart>=0.0.9

# The front end is not a Python dependency: it lives in web/ and is built with
# npm. See the README for the two commands that run it.

# Document search is optional and lives in requirements-rag.txt. It pulls in
# torch, which is larger than everything above put together, so installing it
# is a separate decision rather than the price of installing this.
